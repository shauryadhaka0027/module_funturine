import { Request } from 'express';
import { Document, Types } from 'mongoose';

// Base User Interface
export interface IUser extends Document { _id } { username } {
  productCode, string>;
  warranty= any> {
  message